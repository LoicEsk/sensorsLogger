<form method="post" action="{{ path('sensor_data_delete', {'id': sensor_datum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="my-3">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sensor_datum.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
