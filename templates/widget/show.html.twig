{% extends 'base.html.twig' %}

{% block title %}Widget{% endblock %}

{% block content %}
    <h1>Widget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ widget.id }}</td>
            </tr>
            <tr>
                <th>Période</th>
                <td>{{ widget.periodeValue }} {{ widget.periodeUnit }}</td>
            </tr>
            <tr>
                <th>EndTime</th>
                <td>{{ widget.endTime }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ widget.position }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ widget.size }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ widget.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-3 mb-3">
        <a href="{{ path('widget_index') }}" class="btn btn-outline-primary">back to list</a>
        <a href="{{ path('widget_edit', {'id': widget.id}) }}" class="btn btn-primary">edit</a>
    </div>


    {{ include('widget/_delete_form.html.twig') }}
{% endblock %}
