{% extends 'base.html.twig' %}

{% block title %}Widget{% endblock %}

{% block content %}
    <h1>Widget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ widget.id }}</td>
            </tr>
            <tr>
                <th>StartTime</th>
                <td>{{ widget.startTime }}</td>
            </tr>
            <tr>
                <th>EndTime</th>
                <td>{{ widget.endTime }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ widget.position }}</td>
            </tr>
            <tr>
                <th>Size</th>
                <td>{{ widget.size }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ widget.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('widget_index') }}">back to list</a>

    <a href="{{ path('widget_edit', {'id': widget.id}) }}">edit</a>

    {{ include('widget/_delete_form.html.twig') }}
{% endblock %}
